apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: coldcase-ingress
  namespace: coldcase
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Bandwidth & Cost Optimization
    nginx.ingress.kubernetes.io/use-gzip: "true"
    nginx.ingress.kubernetes.io/gzip-types: "text/html text/css text/plain text/xml application/xml application/javascript application/json"
spec:
  tls:
    - hosts:
        - ccase.tech
        - api.ccase.tech
      secretName: coldcase-tls
  rules:
    - host: ccase.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    - host: api.ccase.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 3000
